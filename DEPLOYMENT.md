# Инструкция по загрузке темы Balanz на хостинг TimeWeb

## ✅ Предзагрузочная подготовка (ВЫПОЛНЕНО)

- ✅ Все ссылки на страницы используют динамические WordPress функции
- ✅ Удалены console.log из JavaScript
- ✅ Добавлено автоматическое создание страницы About Us при активации темы
- ✅ Настроено автоматическое удаление дефолтных WordPress страниц и постов

## 📋 Что нужно подготовить

1. **Доступы к хостингу TimeWeb:**
   - URL панели управления
   - Логин и пароль
   - FTP данные

2. **Домен:**
   - Убедитесь что домен привязан к хостингу

## 🚀 Пошаговая инструкция

### Шаг 1: Соберите production-версию

Выполните в терминале в папке темы:

```bash
npm run build
```

Это создаст оптимизированные файлы в `assets/dist/`

### Шаг 2: Экспорт базы данных из Local by Flywheel

1. Откройте **Local by Flywheel**
2. Кликните на сайт **balanz**
3. Перейдите на вкладку **Database**
4. Нажмите **Open Adminer** (откроется в браузере)
5. В Adminer:
   - Нажмите **Export** в левом меню
   - Формат: **SQL**
   - Опции: оставьте по умолчанию (DROP + CREATE + INSERT)
   - Нажмите **Export**
6. Сохраните файл как `balanz_database.sql`

### Шаг 3: Подготовка базы данных на TimeWeb

1. Войдите в **панель управления TimeWeb**
2. Найдите раздел **Базы данных** → **MySQL**
3. Создайте новую базу данных:
   - Нажмите **Создать базу данных**
   - Запишите **имя БД** (например: `u1234567_balanz`)
   - Запишите **имя пользователя** (например: `u1234567_admin`)
   - Запишите **пароль** (создайте надежный)
   - Нажмите **Создать**
4. Откройте **phpMyAdmin** для этой БД
5. Выберите вашу базу данных слева
6. Перейдите на вкладку **Импорт**
7. Выберите файл `balanz_database.sql`
8. Нажмите **Вперёд** (импорт займёт несколько секунд)

### Шаг 4: Замена URL в базе данных

**ВАЖНО:** Узнайте ваш локальный URL. В Local by Flywheel обычно это `http://balanz.local`

В phpMyAdmin перейдите на вкладку **SQL** и выполните следующие запросы:

```sql
-- Замените URL (измените на ваши реальные URL!)
-- ЗАМЕНИТЕ: balanz.local - на ваш локальный URL из Local
-- ЗАМЕНИТЕ: вашдомен.ru - на ваш реальный домен

UPDATE wp_options 
SET option_value = 'https://вашдомен.ru' 
WHERE option_name = 'siteurl' OR option_name = 'home';

UPDATE wp_posts 
SET post_content = REPLACE(post_content, 'http://balanz.local', 'https://вашдомен.ru');

UPDATE wp_posts 
SET guid = REPLACE(guid, 'http://balanz.local', 'https://вашдомен.ru');

UPDATE wp_postmeta 
SET meta_value = REPLACE(meta_value, 'http://balanz.local', 'https://вашдомен.ru');
```

Если у вас другой префикс таблиц (не `wp_`), замените `wp_` на ваш префикс.

### Шаг 5: Загрузка файлов WordPress на хостинг

**Вариант А: Через FTP-клиент FileZilla (Рекомендуется)**

1. Скачайте и установите [FileZilla](https://filezilla-project.org/)
2. Подключитесь к хостингу:
   - Хост: `ftp.вашдомен.ru` (см. в панели TimeWeb)
   - Имя пользователя: ваш FTP логин
   - Пароль: ваш FTP пароль
   - Порт: 21
3. Перейдите в папку вашего сайта:
   - Обычно это `public_html` или `www`
4. Загрузите ВСЕ файлы из папки:
   ```
   C:\Users\miras\Local Sites\balanz\app\public\
   ```
5. Загрузка займёт 10-30 минут в зависимости от скорости интернета

**Вариант Б: Через файловый менеджер TimeWeb**

1. В панели TimeWeb найдите **Файловый менеджер**
2. Перейдите в папку сайта (`public_html`)
3. Загрузите файлы (можно архивом):
   - Создайте ZIP-архив папки `public`
   - Загрузите на хостинг
   - Распакуйте через файловый менеджер

### Шаг 6: Настройка wp-config.php

1. Откройте файл `wp-config.php` на хостинге (через FileZilla или файловый менеджер)
2. Найдите строки подключения к БД и измените на ваши данные:

```php
// ** Настройки базы данных ** //
define('DB_NAME', 'u1234567_balanz');     // Ваше имя БД
define('DB_USER', 'u1234567_admin');      // Ваш пользователь БД
define('DB_PASSWORD', 'ваш_пароль');      // Ваш пароль БД
define('DB_HOST', 'localhost');           // Обычно localhost
define('DB_CHARSET', 'utf8mb4');
define('DB_COLLATE', '');
```

3. Добавьте эти строки для HTTPS (перед строкой `/* Это всё, дальше не редактируем */`):

```php
// HTTPS и безопасность
define('FORCE_SSL_ADMIN', true);
if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
    $_SERVER['HTTPS'] = 'on';
}
```

4. Сохраните файл

### Шаг 7: Проверка и настройка .htaccess

1. Проверьте наличие файла `.htaccess` в корне сайта
2. Если его нет, создайте со следующим содержимым:

```apache
# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>
# END WordPress

# Безопасность
<Files wp-config.php>
order allow,deny
deny from all
</Files>
```

### Шаг 8: Установка SSL-сертификата

1. В панели TimeWeb найдите **SSL-сертификаты**
2. Выберите ваш домен
3. Включите **Let's Encrypt** (бесплатный SSL)
4. Дождитесь активации (обычно 5-15 минут)
5. Включите **автоматический редирект с HTTP на HTTPS**

### Шаг 9: Первый вход в WordPress

1. Откройте в браузере: `https://вашдомен.ru/wp-admin`
2. Введите логин и пароль от вашей локальной копии
3. Если не получается войти, сбросьте пароль:
   - В phpMyAdmin найдите таблицу `wp_users`
   - Найдите вашего пользователя
   - Измените поле `user_pass` на: `$P$BwJ1eDzqHp6qE9XhD9vVq0jZqJ8F3f/`
   - Это установит пароль: `admin123`
   - Войдите и сразу смените пароль!

### Шаг 10: Проверка работы сайта

После входа проверьте:

- ✅ Главная страница открывается
- ✅ Страница About Us открывается (автоматически создана)
- ✅ Меню работает корректно
- ✅ Все изображения загружаются
- ✅ CSS и JavaScript работают

### Шаг 11: Настройка темы

1. Перейдите в **Внешний вид** → **Темы**
2. Убедитесь что тема **Balanz** активирована
3. Перейдите в **Theme Settings** (в левом меню админки)
4. Заполните настройки:
   - **App Links**: ссылки на скачивание приложения
   - **Social Links**: ссылки на соцсети (Facebook, Instagram, LinkedIn, X)

### Шаг 12: Установка и настройка плагинов

**Обязательные плагины:**

1. **Advanced Custom Fields (ACF) PRO**
   - Скачайте и установите
   - Активируйте
   - Импортируйте поля из `acf-json` (автоматически)

**Рекомендуемые плагины:**

2. **Yoast SEO** - для SEO оптимизации
3. **W3 Total Cache** или **WP Super Cache** - кеширование
4. **Wordfence Security** - безопасность
5. **UpdraftPlus** - резервное копирование

### Шаг 13: Финальная проверка

- ✅ Откройте сайт в режиме инкогнито
- ✅ Проверьте работу на мобильных устройствах
- ✅ Проверьте скорость загрузки: [PageSpeed Insights](https://pagespeed.web.dev/)
- ✅ Проверьте SSL: [SSL Labs](https://www.ssllabs.com/ssltest/)

## ⚠️ Важные замечания для хостинга

1. **Права доступа к файлам:**
   - Папки: 755
   - Файлы: 644
   - wp-config.php: 600

2. **Лимиты PHP:**
   - Если сайт медленный, увеличьте в панели TimeWeb:
   - `memory_limit`: 256M
   - `max_execution_time`: 300
   - `upload_max_filesize`: 64M

3. **Резервное копирование:**
   - Настройте автоматические бэкапы в панели TimeWeb
   - Или используйте плагин UpdraftPlus

## 🐛 Решение проблем

### Проблема: "Error establishing database connection"
**Решение:** Проверьте данные в wp-config.php

### Проблема: Белый экран
**Решение:** Включите отображение ошибок в wp-config.php:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

### Проблема: Страницы 404
**Решение:** Перейдите в **Настройки** → **Постоянные ссылки** и нажмите **Сохранить**

### Проблема: CSS/JS не загружаются
**Решение:** Проверьте права доступа к папке `wp-content`

## 📞 Поддержка TimeWeb

- Сайт: https://timeweb.com/ru/
- Телефон: 8 (800) 700-06-08
- Email: support@timeweb.ru
- Документация: https://timeweb.com/ru/help/

## ✅ После успешного запуска

1. Отключите режим отладки в wp-config.php:
```php
define('WP_DEBUG', false);
```

2. Настройте кеширование (плагин W3 Total Cache)

3. Настройте регулярные бэкапы

4. Добавьте сайт в Google Search Console и Google Analytics

---

**Дата создания:** 16 января 2026  
**Версия темы:** 1.0.0

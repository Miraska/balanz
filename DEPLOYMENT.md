# 🚀 Деплой Balanz Theme на Production

Руководство по развертыванию темы на продакшн сервере.

## 📋 Подготовка к деплою

### 1. Production сборка

Локально выполните production сборку:

```bash
cd wp-content/themes/balanz
npm run build
```

Это создаст оптимизированные файлы в `assets/dist/`:

- Минифицированный CSS
- Минифицированный JS
- Оптимизированные ресурсы

### 2. Что загружать на сервер

Загрузите **всю папку темы**, включая:

```
✅ wp-content/themes/balanz/
   ├── assets/dist/          # Собранные файлы
   ├── template-parts/       # PHP шаблоны
   ├── acf-json/            # ACF поля (опционально)
   ├── *.php                # Все PHP файлы
   ├── style.css            # Мета-информация темы
   └── screenshot.png       # Скриншот темы
```

**НЕ загружайте:**

```
❌ node_modules/
❌ assets/src/             # Исходники (не нужны на проде)
❌ package.json            # Локальная разработка
❌ vite.config.js          # Локальная разработка
❌ .git/
```

---

## 🗄 Методы деплоя

### Метод 1: FTP/SFTP (для большинства хостингов)

1. Подключитесь к серверу через FTP клиент (FileZilla, Cyberduck)
2. Перейдите в `wp-content/themes/`
3. Загрузите папку `balanz/`
4. Убедитесь, что права доступа корректные (755 для папок, 644 для файлов)

### Метод 2: Git Deploy (рекомендуется)

Если на сервере есть Git:

```bash
# На сервере
cd /path/to/wordpress/wp-content/themes/
git clone https://github.com/your-repo/balanz.git

# Соберите файлы (если на сервере есть Node.js)
cd balanz
npm install --production
npm run build
```

### Метод 3: SSH + rsync

```bash
# С локальной машины
rsync -avz --exclude 'node_modules' \
  --exclude '.git' \
  --exclude 'assets/src' \
  ./wp-content/themes/balanz/ \
  user@server:/path/to/wp-content/themes/balanz/
```

---

## 🔌 Установка плагинов на Production

### Обязательные плагины

1. **Advanced Custom Fields PRO**

   - Загрузите .zip файл плагина
   - Установите через WP Admin → Плагины → Добавить новый → Загрузить
   - Активируйте

2. **WP Mail SMTP** (для форм)
   - Установите через репозиторий WordPress
   - Настройте SMTP (Gmail, SendGrid, etc.)

### Рекомендуемые плагины

- **Yoast SEO** или **RankMath** — SEO оптимизация
- **Wordfence** или **Sucuri** — безопасность
- **WP Rocket** или **W3 Total Cache** — кэширование
- **Smush** или **ShortPixel** — оптимизация изображений

---

## ⚙️ Настройка на Production

### 1. Активация темы

```
WP Admin → Внешний вид → Темы → Активировать "Balanz"
```

### 2. Импорт ACF полей

**Вариант A: ACF JSON Sync (рекомендуется)**

Если вы скопировали папку `acf-json/` с полями:

```
WP Admin → ACF → Field Groups → Sync
```

**Вариант B: Ручная настройка**

См. инструкцию в `ACF-FIELDS-SETUP.md`

### 3. Настройка глобальных параметров

```
WP Admin → Настройки темы → Ссылки на приложение
- App Store Link: [ссылка]
- Google Play Link: [ссылка]

WP Admin → Настройки темы → Контакты
- Email для форм: [email]
- Соцсети: [ссылки]
```

### 4. Заполнение контента

```
WP Admin → Страницы → Главная → Редактировать
```

Заполните все ACF поля контентом.

---

## 📧 Настройка почты

### Gmail SMTP (для малого трафика)

1. Установите **WP Mail SMTP**
2. Настройте:

   ```
   SMTP Host: smtp.gmail.com
   SMTP Port: 587
   Encryption: TLS
   Username: your-email@gmail.com
   Password: [Пароль приложения]
   ```

3. Создайте пароль приложения:
   - Google Account → Security → 2-Step Verification
   - App passwords → Generate

### SendGrid (для большого трафика)

1. Зарегистрируйтесь на [SendGrid](https://sendgrid.com/)
2. Получите API ключ
3. Настройте в WP Mail SMTP:
   ```
   Mailer: SendGrid
   API Key: [ваш ключ]
   ```

---

## 🔒 Безопасность

### 1. WordPress Security

Добавьте в `wp-config.php`:

```php
// Отключить редактор файлов
define('DISALLOW_FILE_EDIT', true);

// Безопасные ключи
define('AUTH_KEY',         'сгенерируйте на https://api.wordpress.org/secret-key/1.1/salt/');
// ... остальные ключи
```

### 2. Защита .htaccess

Добавьте в `.htaccess`:

```apache
# Защита wp-config.php
<files wp-config.php>
order allow,deny
deny from all
</files>

# Защита от hotlinking
RewriteEngine on
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^http(s)?://(www\.)?yourdomain.com [NC]
RewriteRule \.(jpg|jpeg|png|gif|svg)$ - [NC,F,L]
```

### 3. Обновления

Регулярно обновляйте:

- WordPress core
- Плагины
- PHP версию (минимум 8.2)

---

## ⚡ Оптимизация производительности

### 1. Кэширование

Установите плагин кэширования:

- **WP Rocket** (платный, но лучший)
- **W3 Total Cache** (бесплатный)

Настройки:

```
✅ Page cache
✅ Browser cache
✅ Minify CSS/JS
✅ Gzip compression
✅ Lazy load images
```

### 2. CDN (опционально)

Для глобальной аудитории:

- **Cloudflare** (бесплатный plan)
- **BunnyCDN** (платный, быстрый)

### 3. Оптимизация изображений

- Используйте WebP формат
- Сжимайте изображения (плагин Smush/ShortPixel)
- Используйте правильные размеры

### 4. Database оптимизация

```sql
-- Очистка ревизий
DELETE FROM wp_posts WHERE post_type = 'revision';

-- Очистка корзины
DELETE FROM wp_posts WHERE post_status = 'trash';

-- Оптимизация таблиц
OPTIMIZE TABLE wp_posts, wp_postmeta, wp_options;
```

Или используйте плагин **WP-Optimize**.

---

## 🧪 Проверка перед запуском

### Чеклист

- [ ] Тема активирована и работает
- [ ] Все плагины установлены и активированы
- [ ] ACF поля настроены
- [ ] Контент заполнен
- [ ] Форма обратной связи работает
- [ ] Почта настроена и тестовое письмо отправлено
- [ ] Ссылки на App Store / Google Play работают
- [ ] Сайт адаптивный на мобильных
- [ ] Анимации работают
- [ ] Мета-теги (SEO) заполнены
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Google Analytics подключен (если нужно)
- [ ] Backup настроен

### Тестирование

Проверьте:

```
✅ Chrome / Firefox / Safari
✅ Мобильные устройства (iPhone, Android)
✅ Планшеты
✅ Скорость загрузки (PageSpeed Insights)
✅ Консоль браузера (нет ошибок)
```

---

## 📊 Мониторинг

### Google Analytics (опционально)

Добавьте в `header.php` перед `</head>`:

```php
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### Uptime мониторинг

Используйте:

- **UptimeRobot** (бесплатный)
- **Pingdom**

---

## 🔄 Backup

### Автоматический backup

Используйте плагины:

- **UpdraftPlus** (бесплатный)
- **BackupBuddy** (платный)

Настройте:

```
✅ Ежедневный backup базы данных
✅ Еженедельный backup файлов
✅ Хранение на удаленном сервере (Google Drive, Dropbox)
```

### Ручной backup

```bash
# Backup базы данных
mysqldump -u username -p database_name > backup.sql

# Backup файлов
tar -czf wordpress-backup.tar.gz /path/to/wordpress/
```

---

## 🆘 Troubleshooting

### Белый экран (WSOD)

1. Проверьте лог ошибок PHP
2. Отключите плагины
3. Переключитесь на дефолтную тему
4. Проверьте память PHP (минимум 256MB)

### Форма не отправляется

1. Проверьте настройки SMTP
2. Отключите временно плагины безопасности
3. Проверьте логи сервера

### Медленная загрузка

1. Включите кэширование
2. Оптимизируйте изображения
3. Используйте CDN
4. Проверьте плагины (отключите ненужные)

---

## 📞 Поддержка

После деплоя:

- Мониторьте ошибки первую неделю
- Соберите обратную связь от пользователей
- Оптимизируйте производительность

---

**Успешного деплоя! 🚀**

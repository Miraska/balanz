# Contributing to Balanz Theme

Спасибо за интерес к проекту! Это руководство поможет вам внести вклад в разработку.

## 🚀 Начало работы

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Следуйте code style проекта
4. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
5. Пушьте в ветку (`git push origin feature/amazing-feature`)
6. Создайте Pull Request

## 📝 Code Style

### PHP
- PSR-12 стандарт
- Отступы: 4 пробела
- Используйте type hints
- Документируйте функции PHPDoc

```php
/**
 * Описание функции
 *
 * @param string $param Описание параметра
 * @return bool
 */
function example_function(string $param): bool {
    // код
}
```

### JavaScript
- ES6+ синтаксис
- Отступы: 2 пробела
- Используйте const/let (не var)
- Документируйте сложную логику

```javascript
/**
 * Инициализация модуля
 */
export function initModule() {
  // код
}
```

### SCSS
- BEM-подобная методология
- Отступы: 2 пробела
- Используйте переменные из `_variables.scss`
- Группируйте свойства логически

```scss
.component {
  // Positioning
  position: relative;
  
  // Box model
  display: flex;
  width: 100%;
  
  // Typography
  font-size: $font-size-base;
  
  // Visual
  background-color: $color-white;
  
  // Misc
  transition: all $transition-base;
}
```

## 🧪 Тестирование

Перед коммитом:
1. Проверьте код на синтаксические ошибки
2. Протестируйте на разных разрешениях
3. Проверьте в разных браузерах
4. Убедитесь, что сборка проходит успешно

```bash
npm run build
```

## 📋 Commit Messages

Используйте понятные сообщения коммитов:

```
feat: добавить новую секцию FAQ
fix: исправить баг с формой
style: обновить стили кнопок
docs: обновить README
refactor: рефакторинг модуля анимаций
perf: оптимизация загрузки изображений
```

## 🐛 Баг-репорты

При создании issue укажите:
- Описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Актуальное поведение
- Скриншоты (если применимо)
- Окружение (браузер, версия WP, версия темы)

## 💡 Feature Requests

Предложения новых фич приветствуются! Опишите:
- Что вы хотите добавить
- Зачем это нужно
- Примеры использования

## 📞 Вопросы?

Если у вас есть вопросы по разработке, создайте issue с меткой `question`.

---

Спасибо за вклад в проект! 🎉

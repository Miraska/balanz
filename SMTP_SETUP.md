# 📧 Настройка SMTP для отправки email (Timeweb)

## Проблема
Форма работает, но письма не приходят на почту, потому что:
- Timeweb блокирует PHP `mail()` функцию
- Нужно использовать SMTP для отправки

---

## ✅ Решение 1: Через админку WordPress (рекомендуется)

### Шаг 1: Создайте почтовый ящик на Timeweb

1. Зайдите в панель Timeweb
2. Перейдите в **Почта → Почтовые ящики**
3. Создайте ящик типа `noreply@ваш-домен.ru`
4. Запомните пароль

### Шаг 2: Настройте SMTP в WordPress

1. Зайдите в **Theme Settings → Form Settings**
2. Прокрутите до раздела **"SMTP Settings"**
3. Заполните поля:

```
SMTP Host: smtp.timeweb.ru
SMTP Port: 587
SMTP Username: noreply@ваш-домен.ru
SMTP Password: [пароль от почты]
From Email: noreply@ваш-домен.ru
From Name: Balanz
```

### Шаг 3: Проверьте настройки

1. В поле **"Test Email"** введите ваш email
2. Нажмите **"Update"** (сохранить)
3. Проверьте почту — должно прийти тестовое письмо

### Шаг 4: Укажите email для форм

В **Theme Settings → Form Settings** в поле **"Recipient Email"** укажите email, куда должны приходить заявки с формы

---

## ✅ Решение 2: Через плагин WP Mail SMTP (альтернатива)

Если не хотите настраивать SMTP в теме, установите плагин:

1. **Plugins → Add New**
2. Найдите **"WP Mail SMTP by WPForms"**
3. Установите и активируйте
4. Перейдите в **Settings → WP Mail SMTP**
5. Настройте так же, как выше

**Преимущество:** Более продвинутые настройки, логи отправок, поддержка Gmail/SendGrid

---

## 🔧 Настройки SMTP для разных сервисов

### Timeweb
```
Host: smtp.timeweb.ru
Port: 587 (TLS) или 465 (SSL)
Username: your-email@yourdomain.ru
Password: ваш пароль
```

### Gmail (если используете Gmail)
```
Host: smtp.gmail.com
Port: 587
Username: your-email@gmail.com
Password: App Password (не обычный пароль!)
```

**Для Gmail:** Создайте App Password в настройках Google Account → Security → App passwords

### Yandex Mail
```
Host: smtp.yandex.ru
Port: 465 (SSL)
Username: your-email@yandex.ru
Password: ваш пароль
```

---

## 🐛 Отладка

### Проверка 1: Включите WP_DEBUG

В `wp-config.php` добавьте:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

Логи будут в файле `/wp-content/debug.log`

### Проверка 2: Просмотр заявок

Даже если email не отправляется, все заявки сохраняются:
- **WordPress Admin → Form Submissions**
- Там вы увидите все отправленные формы

### Проверка 3: Тест SMTP через консоль

В консоли браузера (F12) после отправки формы смотрите:
```javascript
// Если email не отправился, будет:
{
  success: true,
  data: {
    message: "...",
    debug: {
      email_sent: false,
      error: "описание ошибки"
    }
  }
}
```

---

## ❓ FAQ

### Q: Письма попадают в спам
**A:** Убедитесь, что:
- From Email — с вашего домена (`noreply@yourdomain.ru`)
- Настроены SPF и DKIM записи для домена (в панели Timeweb)

### Q: Ошибка "Could not authenticate"
**A:** Проверьте:
- Правильность логина и пароля
- Что почтовый ящик создан в панели Timeweb
- Порт (587 для TLS, 465 для SSL)

### Q: Тестовое письмо приходит, но с формы — нет
**A:** Проверьте:
- Заполнено ли поле "Recipient Email" в Theme Settings → Form Settings
- Email валидный
- Нет ли блокировки на хостинге (свяжитесь с поддержкой Timeweb)

### Q: Все настроил, но всё равно не работает
**A:** Напишите в поддержку Timeweb — попросите:
- Разблокировать отправку email через SMTP
- Проверить, не заблокирован ли порт 587/465
- Узнать правильные настройки SMTP для вашего аккаунта

---

## 🎉 Готово!

После настройки SMTP все письма с формы будут приходить на указанный email.

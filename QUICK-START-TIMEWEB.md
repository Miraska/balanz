# 🚀 Быстрый старт на хостинге TimeWeb

## ⚡ Краткая инструкция (5 шагов)

### 1️⃣ Подготовка (на локальной машине)

```bash
# В папке темы выполните:
npm run build
```

Узнайте ваш локальный URL в Local by Flywheel (например: `http://balanz.local`)

### 2️⃣ Экспорт базы данных

Local → Database → Open Adminer → Export → Сохранить SQL

### 3️⃣ На хостинге TimeWeb

**База данных:**
1. Создать MySQL базу данных
2. Открыть phpMyAdmin → Импорт → загрузить SQL
3. Вкладка SQL → выполнить:

```sql
UPDATE wp_options SET option_value = 'https://ВАШ-ДОМЕН.ru' 
WHERE option_name = 'siteurl' OR option_name = 'home';

UPDATE wp_posts SET post_content = REPLACE(post_content, 'http://balanz.local', 'https://ВАШ-ДОМЕН.ru');
UPDATE wp_posts SET guid = REPLACE(guid, 'http://balanz.local', 'https://ВАШ-ДОМЕН.ru');
UPDATE wp_postmeta SET meta_value = REPLACE(meta_value, 'http://balanz.local', 'https://ВАШ-ДОМЕН.ru');
```

### 4️⃣ Загрузка файлов

**FileZilla:**
- Хост: `ftp.ваш-домен.ru`
- Порт: 21
- Загрузить все из: `C:\Users\miras\Local Sites\balanz\app\public\`
- В папку: `public_html`

**Отредактировать wp-config.php:**

```php
define('DB_NAME', 'ваша_база');
define('DB_USER', 'ваш_пользователь');
define('DB_PASSWORD', 'ваш_пароль');
define('DB_HOST', 'localhost');

// Добавить:
define('FORCE_SSL_ADMIN', true);
if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
    $_SERVER['HTTPS'] = 'on';
}
```

### 5️⃣ Финал

1. TimeWeb → SSL → Let's Encrypt → Активировать
2. Открыть: `https://ваш-домен.ru/wp-admin`
3. Войти (используйте ваш локальный логин/пароль)
4. Внешний вид → Темы → Balanz активирована ✅
5. Установить плагин **ACF (Advanced Custom Fields)**
6. Theme Settings → заполнить настройки

---

## ✅ Проверка

- [ ] Главная страница открывается
- [ ] About Us открывается
- [ ] Меню работает
- [ ] Изображения загружаются
- [ ] SSL активен (замок в браузере)

---

## 📞 Поддержка TimeWeb

**Телефон:** 8 (800) 700-06-08  
**Email:** support@timeweb.ru

---

## 📄 Полная документация

См. файл **DEPLOYMENT.md** для подробной инструкции
